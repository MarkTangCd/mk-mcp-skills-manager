{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "fixture-fs": {
      "type": "local",
      "command": ["echo", "fixture-fs"],
      "enabled": true,
      "environment": { "FIXTURE_TOKEN": "${FAKE_TOKEN}" }
    },
    "fixture-remote": {
      "type": "remote",
      "url": "https://example.invalid/fixture",
      "enabled": false
    }
  }
}
